I have enough context. Writing the dreaming report to stdout now.

---

# dreaming pass — v2v-demo (2026-09-13)

Read-only. No file changes. Reviewed `AGENTS.md`, `.agents/plan.md`,
`.agents/changes.md`, `docs/requirements.md`, `docs/architecture.md`,
`cmd/bot/`, `internal/dialog/`, `internal/store/`, `cmd/source-status-backfill/`,
`topics/`, `minions/`, memory/, and `git log --since="2 weeks ago"`. The repo
is in a tight post-`playful-squishing-fox` state — Stages 3–6 of the multi-
topic plan all landed 2026-09-06, brand renames and the closed-note handoff
followed, the W36 dreaming pass was applied in commit `d71c9fe`. Health is
good; findings below are mostly small or already on the radar.

## Blocker

(none)

## Should-fix

### 1. `tmp/model-compare.py` is a stale Python tool from an earlier era
`AGENTS.md` line 21–22 says **`minions/` — dev helper scripts that get
reused (tracked; Bash, not Python)**. `tmp/model-compare.py` (3.2 KB, in
`tmp/`) is Python, sits next to other `tmp/` scratch, and was last touched
in the pre-multi-topic era. Per `AGENTS.md`'s own rule and the user's
project-level "avoid Python for user-facing scripts" instruction in
`CLAUDE.md`, either port it to Go/Bash, promote it to `minions/` if still
useful, or delete. Tracked scripts in `minions/` are all Bash/Go today.

> [applied 2026-09-20: removed — script dead (references pre-multi-topic
> `prompt/system.md` / `kb/translation-bureau.md`, no port possible)]

### 2. `tmp/Агенції_нерухомості.md`, `Клінінг.md`, `СТО_автосервіс.md`,
`Стоматклініки.md` — same `tmp/` issue, 8–11 KB each, Cyrillic filenames.
Pre-Stages-3–6 KB research notes (referenced by the KB council review
brief, since the council reviews were content-only and the actual
`topics/<id>/kb.md` files are much larger). They served their purpose; if
Val doesn't need them as provenance, drop them so `tmp/` stays "throwaway
scratch."

> [applied 2026-09-20: removed]

### 3. `.agents/changes.md` is NOT tracked and lives in `tmp/` style state
`.gitignore` line 38 lists `.agents/changes.md` as ignored — it's the
"build-time trail" mentioned in `project-v2v-demo.md`. Two practical
implications:

- The recent **dental sedation fix** (commit `993a5cb`, "sedation /
  уві сні escalates instead of collecting a booking"), the **3 brand
  renames** (`f5fb8e8` + `4f6f366` + `1e71361`), the **escalate-after-
  hours note** (`08f8cab`), and the **off-KB recipient / off-topic-twice**
  fix (`6476f59`) all live in `.agents/changes.md` but **only on the
  machine that did the work** — they don't travel with the repo. So an
  auditor reading `git log` + `git show <hash>` gets the diff but not the
  reasoning. AGENTS.md §"Conventions" line 17 ("Package layout is fixed
  by `.agents/plan.md` — do not restructure it without updating that doc
  in the same commit") explicitly does not require updating changes.md;
  but changes.md is the only place these "why" decisions live.
- Suggestion: either track it (drop from `.gitignore`) or accept its
  machine-local nature — but pick deliberately. The W36 dreaming pass
  was applied via commit `d71c9fe` precisely because changes.md wasn't
  available across machines; tracking it would let `d71c9fe` go away.

### 4. `cmd/source-status-backfill/` — undocumented in `.agents/plan.md`
A `cmd/` subdirectory for an operator tool that's **not** `cmd/bot/`.
`AGENTS.md` and `.agents/plan.md` only mention `cmd/bot/main.go`.
`find -type d` shows `./cmd/bot` (per plan) plus — from the 2026-09-12
session context — `cmd/source-status-backfill/main.go`. It's a separate
operator tool (cursor pagination, `IsTerminal` skip, soft-fail on `cur.Err()`,
referenced "external_id as lastSeen"), not part of the bot, so it doesn't
violate the package layout — but `.agents/plan.md`'s **Package layout**
section (line 44–93) doesn't mention it. Either add a one-liner to the
plan ("`cmd/source-status-backfill/` — operator CLI; not built by
`make check`") or split it out (it has no connection to the bot, so it
arguably belongs in `~/wrk/` elsewhere, not in this repo at all).

## Nit

### 5. `project-v2v-demo.md` is 4 days stale
Project memory says **"DONE 2026-09-06"** but today is 2026-09-13 and
several things have landed since: 3 brand renames (`f5fb8e8`, `4f6f366`,
`1e71361`), the picker portfolio links (`2786236`), the bilingual button
trim (`b4dc04b`), the closed-note handoff (`08f8cab`), the dental sedation
fix (`993a5cb`), the off-topic-twice fix (`6476f59`), the W36 dreaming pass
applied (`d71c9fe`), and `glm-5.3:cloud` added to the model list
(`1ba03f0`). None of these are reflected in memory — including the fact
that `.agents/changes.md` records them but memory itself doesn't. The
deliverable-state section still says "the i10-sample.mp3 + transcript were
sent to Ftb Translation" (true at v0.1.0) — that's still accurate.
Suggestion: a one-line memory bump on the next session touch.

> [applied 2026-09-20: memory updated with a "Post-2026-09-15 work"
> section covering runtime failover (dialog/STT/TTS, 3 commits),
> whisper.cpp opt-in STT, Skoda TTS fix, and the Oracle Cloud Free Tier
> deploy doc with shopogoda co-hosting. Description line refreshed,
> modified → 2026-09-20.]

### 6. `README.md` slot-count example still claims 2 slots (per commit `becfb0d`)
Commit `becfb0d` ("docs(topics): mark the README slots example abbreviated;
reword fallback") closed this — worth a glance to confirm the fix landed
cleanly. (Heuristic only; not opened.)

### 7. `opencode.json` model additions — model defaults drift across envs
Commit `1ba03f0` ("opencode.json: add glm-5.3:cloud to the model list")
adds `glm-5.3:cloud`. `.env.example` line 60–62 mentions
`glm-5.3-flash:cloud` as an override example. Minor naming inconsistency:
`glm-5.3:cloud` vs `glm-5.3-flash:cloud`. Either a different model or a
typo. Worth verifying on the next touch.

### 8. Gate-tuning fragility is still the recurring theme
Per `.agents/changes.md`: at least **5 distinct fixes** in the last two
weeks (sedation/уві-сні, phone-shaped bypass, hardEscalate manager phrases,
off-topic-twice, off-KB recipient) — all `gate.go`/`dialog.go`/`system.md`
patches to the same content-blind heuristics. The plan's section "B1
fallback" hasn't been triggered yet (no stemmer), and the residual note
"off-topic-twice → escalate only fires reliably when both strikes hit the
gate" is still in `.agents/changes.md`. Per the user's
**iteration-vs-structural** rule (`feedback_iteration_vs_structural.md`):
if a 6th hit lands, step back — the heuristic layer is reaching its
useful limit and a per-topic `GateFloor` / a richer classifier may be the
right structural call. Not a blocker yet; flag for Val.

## Confirmed clean

- **Package layout invariant** holds. `find -type d` matches
  `.agents/plan.md` §"Package layout" line-for-line except for the
  `cmd/source-status-backfill/` noted above.
- **`SESSION_STORE=sqlite` scope**: schema is one `sessions` table (chat_id,
  data, updated_at) — no per-topic tables, no settings/kv tables, no
  general-purpose datastore creep. Still scoped to `Session` data only.
- **Config-gating discipline**: every post-v0.1.0 feature lands behind a
  config switch (`STT_BACKEND=none`, `SESSION_STORE=sqlite`,
  `TOPICS_PATH`, `TTS_BACKEND=none`, `BOT_TIMEZONE`). No default-flip.
- **`docs/architecture.md`** still matches the actual shape (mermaid
  flowchart + package table); per-topic `office` + the closed-note
  escalation are described in §3. `data/turns.jsonl` field set still
  matches `@schema TurnRecord`.
- **`docs/requirements.md`**: `@schema Config` lists `tts_backend:
  Enum["elevenlabs","azure"]` — but the **code default in
  `config.go:74` is `elevenlabs`** and the **runtime accepts `none`** (per
  `cmd/bot/main.go:185` and `docs/architecture.md` §2 row for `tts`).
  The `none` value is in the `validate()` switch (`config.go:123`) but not
  in the `Enum[…]`. Same for `stt_backend`: `Enum["none","local","openai"]`
  is correct. Tiny schema drift — add `"none"` to the TTS enum, or accept
  that requirements doc `Enum[…]` lists the *primary* values and `none` is
  a documented extra (no `@schema` violation, just a comment worth adding).
  Note: between the dreaming pass and the apply date (2026-09-20), `espeak`
  was also added to the TTS enum (commits post-W37).

> [applied 2026-09-20: `"none"` added to the TTS enum;
> current `Enum["elevenlabs","azure","espeak","none"]`]
- **Backend defaults** (`config.go`) match `.env.example` and
  `docs/requirements.md @schema Config`: `TTS_BACKEND=elevenlabs`,
  `STT_BACKEND=local`, `DIALOG_BACKEND=ollama`, `SESSION_STORE=memory`,
  `BOT_TIMEZONE=Europe/Kyiv`, `WHISPER_LANG=uk`, `WHISPER_MODEL=turbo`.
- **Memory files** (4, `project-v2v-demo.md` is the big one) reference
  gpt-4.1-mini / whisper-1 / ElevenLabs Starter — current client stack.
- **No secrets in code or logs** observed. `.gitignore` correctly catches
  `.env`, `.env.*`, `*.db`, `*.log`, `data/`, `tmp/`, `.engage/`.
- **`minions/`** scripts (`tts-audition.sh`, `dialog-probe/`, `i10-sample/`,
  `tgdrive/`, `council/`) are all Bash or Go, tracked, each standalone,
  each documented in `minions/TOOLS.md` and the SessionStart hook context.

## Bottom line

No blockers, no architectural drift, no plan-vs-code mismatch. The repo
is healthy and the multi-topic work that landed since the last dreaming
pass (2026-09-06) is consistent with the plan and well-tested via
`minions/dialog-probe`. The **should-fix** items are housekeeping:
`tmp/` cleanup, the `.agents/changes.md` ignore-vs-track decision, and
`cmd/source-status-backfill/`'s absence from `.agents/plan.md`. The
**gate-tuning fragility** is the only recurring pattern worth watching —
if it bites again, escalate to a structural call per the
iteration-vs-structural rule.

# Dreaming pass — v2v-demo (2026-09-06)

Read-only. Findings ranked by severity.

---

## BLOCKER

_None._ `make check` is green (per the previous session's note), no docs-vs-code mismatch is severe enough to block today's work.

---

## SHOULD-FIX

### 1. `.env.example` still advertises `gpt-4o-mini` as the openai default
- **Where:** `.env.example:60` — `Blank → the backend's default (ollama gemma4:cloud, openai gpt-4o-mini, gemini gemini-flash-latest)`
- **Drift:** `internal/dialog/openai.go:18` defaults to **`gpt-4.1-mini`** (D-20, 2026-09-03), and `project-v2v-demo.md` documents that4.1-mini replaced4o-mini because of cert/grounding violations. `.agents/changes.md` records the same. Only `.env.example` (and by extension any new contributor reading it first) still points at the old model.
- **Action:** in `.env.example`, replace `openai gpt-4o-mini` → `openai gpt-4.1-mini`. Trivial — fix in the next commit that touches any env doc.
> [applied 2026-09-06: `.env.example` DIALOG_MODEL comment → `openai gpt-4.1-mini`]


### 2. `docs/requirements.md` REQ-DLG-15 still names `gpt-4o-mini` as the trigger
- **Where:** `docs/requirements.md:32` — `dialog_backend @constraint(...)` narrative: "gpt-4.1-mini (gpt-4o-mini was tried first but …)" — already correct.
- **Where:** `docs/requirements.md:36` — `dialog_model @constraint`: says "blank → … openai gpt-4.1-mini, gemini gemini-flash-latest". Already correct.
- **Where:** `docs/requirements.md:329-331` — REQ-DLG-15 narrative: `NewOpenAI sets response_format:{"type":"json_object"} (gpt-4o-mini drops the object on trivial turns otherwise)`. **Reads as if gpt-4o-mini is the current model.** Past tense ("was tried first") isn't there.
- **Action:** in REQ-DLG-15, replace the parenthetical with: `(gpt-4.1-mini drops the object on trivial turns otherwise; the earlier gpt-4o-mini was tried as the default but failed the cert/grounding rules — see D-20)`. Or move the parenthetical into a `// rationale`-style footnote.
> [applied 2026-09-06: REQ-DLG-15 parenthetical reworded to name gpt-4.1-mini + point at D-20]


### 3. `topics/README.md` "Fallback" wording self-contradicts the same line below it
- **Where:** `topics/README.md:73` — *"Fallback (default when unconfigured)."*
- **Where:** `topics/README.md:73` (full line) — *"If `TOPICS_PATH` points at nothing, or the file parses to an empty array, the bot falls back to a single synthetic topic … A one-entry `topics.json` is treated the same way (no picker)"*.
- **Drift:** the **shipped `topics/topics.json` now has TWO entries** (translation + dental, since commit `9b48159`) — so "default when unconfigured" reads as self-contradictory at the file level: there *is* a config; the picker is now on by default. The README text is technically still right about what happens *if* the file is missing or empty, but the headline "Fallback (default when unconfigured)" misleads.
- **Action:** reword to *"Fallback (used only when the manifest is missing or empty)"* — the council-review note in `.agents/changes.md` ("'Fallback (opt-in default)' in README reads as self-contradictory — reword to 'default when unconfigured' if touching the README anyway") points the wrong direction; "default when unconfigured" is itself the contradiction. The cleanest fix is *"Used only when the manifest is missing or empty"* — ship it as part of the Stage 3 wiring that already touches the README's surrounding text.
> [applied 2026-09-06: README → "Fallback (used only when the manifest is missing or empty)"]


### 4. `plan.md` "Package layout" missing the `internal/store/session_*.go` split
- **Where:** `.agents/plan.md:54-67` lists `internal/store/ append-only JSONL: turn records + lead records (DATA_DIR)` as a single package. The actual tree now has `store.go` + `session.go` + `session_sqlite.go` + their tests (per `ls internal/store/`).
- **Drift:** the package did grow (Feature 2 — sessions) and `.agents/changes.md` records the `sessionSchemaVersion` 1→2 migration + `Session.LeadSlots`/`GateStrike` rename. But the package-layout block in `plan.md` still reads as the v0.1.0 shape. Same for the env→field map (it lists `KB_PATH SYSTEM_PROMPT_PATH GREETING_PATH DATA_DIR SESSION_STORE SESSION_DB_PATH TOPICS_PATH`, which is current — but the council review called out `TOPICS_PATH` was missing from an earlier draft, so this drift was previously fixed and is now current).
- **Action:** extend the `internal/store/` line to call out the two files (and what each owns): `store.go` (append-only JSONL), `session.go` (encode/decode + `sessionSchemaVersion`), `session_sqlite.go` (`SQLiteSessions`, SESSION_STORE=sqlite). One-line doc update, fits the "package layout is fixed by `.agents/plan.md`" rule from `AGENTS.md` line16.
> [applied 2026-09-06: plan.md §Package layout — internal/store/ split, per-topic kb/dialog, topics.json = 5 topics, cmd/bot handler/topics split]


### 5. `internal/store/session.go` `sessionSchemaVersion = 2` doesn't actually have a v1→v2 migration path
- **Where:** `internal/store/session.go:13-16` — "Bump it (and add a migration in DecodeSession) if Session's fields ever change incompatibly; today an old/unknown version is just treated as 'not found' — losing a demo session is cheap, silently misreading one is not."
- **Drift:** the v2 bump landed for the `Session.Slots: map[string]string` switch (Stage 1,2026-09-06, per `.agents/changes.md:1035`). The doc-comment says "add a migration in DecodeSession", but `DecodeSession` only checks `row.Version != sessionSchemaVersion` and returns an error — there is no migration branch. Anyone with a pre-Stage-1 `sessions.db` will see the session treated as not-found on next startup (the doc-comment's design, but the migration step the comment promises was skipped).
- **Action:** either (a) accept "losing a demo session is cheap" and update the comment to remove "and add a migration in DecodeSession" — i.e., drop the promise that doesn't exist; or (b) add the v1→v2 migration (translate the old `Slots` struct to the new map). The repo is at v2.0 with no production users, so (a) is the right call — but say so explicitly. Fix in the next commit that touches `session.go`.
> [applied 2026-09-06: session.go comment — dropped the "add a migration" promise, states no migration path by design]


---

## NIT

### 6. `topics/{auto,realestate,cleaning}/kb.md` are committed but only `dental` is wired up
- **Where:** `topics/auto/kb.md` (25 KB), `topics/realestate/kb.md` (32 KB), `topics/cleaning/kb.md` (35 KB) — all bilingual drafts from the Stage1 commit `561d68b`. `topics/dental/` has `system.md` + `greeting.md` (Stage 3 done); `topics/{auto,realestate,cleaning}/` don't.
- **Drift:** none — the staging is intentional and `.agents/changes.md` records it. Mentioned only so the dreaming pass doesn't read "orphaned files" as drift; they're tracked KB drafts waiting for Stages 4–6.
- **Action:** none.

### 7. `cmd/bot/handler.go` is single11 KB file (post-topics)
- The update loop pseudocode in `plan.md` §"cmd/bot update loop" implies one place. After Features 2 + 3, the file has grown to 11 KB and contains: greeting, picker, slash commands, STT/voice/text resolution, topic gate, dialog turn, TTS, send, store calls. No actual structural problem (test coverage is good — `handler_test.go` is 26 KB), but a future split could pull topics/picker into `topics_send.go` or similar. Not actionable yet.
- **Action:** none now; revisit when the per-topic features (Stages 4–6) add more conditional branches.

### 8. `tmp/model-compare.py` is a Python script
- **Where:** `tmp/model-compare.py` (3.2 KB). `AGENTS.md` says "`minions/` — dev helper scripts … (tracked; Bash, not Python)". `tmp/` is the throwaway scratch space.
- **Drift:** the rule is "Bash, not Python", so a Python script in `tmp/` is *not* a violation per `AGENTS.md` — it lives in `tmp/`, not `minions/`. Still worth flagging because the language rule exists, and if this script ever graduates from throwaway, it should either be rewritten in Bash or stay in `tmp/` deliberately.
- **Action:** none required. If promoted, rewrite in Bash first.

### 9. `tmp/old/test-5_{1,2}/` hold raw test audio
- **Where:** `tmp/old/test-5_1/*.ogg` (~17 files, 200-400 KB each), `tmp/old/test-5_2/*.ogg` (~3 files). The corresponding `transcript.txt` exists per test session. The "old/" subdir is the gitignored throwaway area; this is the 2026-08-31 smoke run that produced test-5_1 and test-5_2 fixes.
- **Drift:** none — `tmp/` is throwaway per `AGENTS.md` line 22. Mentioned for the completeness of the package-layout check.
- **Action:** none. (Could be pruned to free ~5 MB, but the transcripts above them remain useful as raw smoke evidence.)

### 10. `topics.json` ships with a `dental` entry — no longer "no picker by default"
- **Where:** `topics/topics.json:20-35` adds the `dental` entry.
- **Cross-ref drift:** `docs/requirements.md:49` (`@schema Config topics_path`) still says *"the repo ships one with a single topic (translation) so no picker appears by default"*. `.agents/changes.md` Stage 3 entry explicitly calls out: **"the picker is now ON by default; smoke hub / translation.md / `.env.example` updated to say so"** — and `.env.example:152` already does. `docs/requirements.md` did **not** get the same update.
- **Action:** in `docs/requirements.md` line 49 (`topics_path` constraint), update *"the repo ships one with a single topic (translation) so no picker appears by default"* → *"the repo ships two (translation + dental) so the picker appears by default; ship a single-entry manifest to opt out"*. Same wording as `.env.example`. One-line doc fix.
> [applied 2026-09-06: requirements.md topics_path rule → "ships five topics … picker by default"]


---

## Already correctly updatedThese came up as candidates during the review and are fine — calling them out so the next pass doesn't re-flag them:

- `plan.md` env→field map includes `TOPICS_PATH` (the council review flagged it; fixed).
- `topics/README.md` slot example is explicitly marked abbreviated (per commit `becfb0d`).
- `internal/dialog/types.go` `OfficeHours` / `Session.Topic` / `Session.LeadSlots` / `Session.GateStrike` json tags match `docs/requirements.md` `@schema Session` and `@schema OfficeHours` — no drift.
- `internal/store/session_sqlite.go` has exactly one table (`sessions`, `chat_id` PK, `data` JSON blob, `updated_at`) — still `Session`-only, no non-session tables (Non-goals scope creep check: clean).
- `isSlotAnswer` no longer special-cases `GateStrike` (2026-09-05 fix per the council flag — `.agents/changes.md:1019-1016`); the code at `internal/dialog/gate.go:242-258` matches the documented new behavior.
- `cmd/bot/handler.go:212,226` still calls `store.AppendTurn` / `store.AppendLead` (JSONL append-only) — no new DB tables, no lead-store split.
- `topics.json` translation entry `slots` array has all six declared keys (`language_pair, doc_type, volume, deadline, certification, delivery`) — matches the plan; the README is now marked abbreviated so a copy-paste can't silently shrink the schema.
- Memory: `project-v2v-demo.md` reflects the **current** state (Stage 1 + Stage 3 done, picker ON by default, `gpt-4.1-mini` as the client default). `MEMORY.md` index is accurate.

---

## Recurring commit themes (last 2 weeks, 50 commits)

For the AGENTS.md-rule-or-test test: three patterns recur.

- **Smoke-test lettered scenarios** (`smoke-test.md 2[a-z]…`, `13[a-g]`, `15[a-d]`, `16[a-k]`, `18`) appear in ~30 of the 50 commits. Each is "scenario X verified → no code change". Pattern: every discovered bug lives at `internal/dialog/{dialog.go,gate.go}` or `topics/translation/system.md` / `kb.md`. Stable enough to keep as-is — the smoke doc is acting as a regression gate, which is exactly its role.
- **Live-smoke → fix → re-verify** shows up on its own (the August passes). One newish pattern: **`minions/dialog-probe`** is the regression shortcut — it lets you run smoke text rows without a real Telegram. ~6 commits use it (`83be0fe`, `8fd7a62`, `fb261ec`, `04055fe`, `3b10503`, `a48c7d0`). It's now the primary verification tool. No AGENTS.md rule needed; `minions/TOOLS.md` already documents it.
- **`feat` / `fix` / `tool` / `content` / `docs` commit prefixes** are consistent. No commit in the last 50 reverted a previous one. No commit was re-pushed. Clean.

No new AGENTS.md rule candidates from the commit stream.

---

## Summary

5 should-fix findings (all small, no design questions), 5 nits (no action needed), 0 blockers. The recurring theme is **doc-code drift on the model default** (`.env.example` + `docs/requirements.md` REQ-DLG-15 still say `gpt-4o-mini`), which is the same one-line edit in two places. Suggest folding all five should-fixes into the Stage 4 commit (next `feat(topics): auto service`) so they ride along with one of the planned per-topic commits — easier to justify than a dedicated docs-only PR.
